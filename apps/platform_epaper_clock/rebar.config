{erl_opts, [debug_info]}.
{deps, [
    {iconv, {git, "https://github.com/processone/iconv.git", {branch, "master"}}}
]}.

{shell, [
  % {config, "config/sys.config"},
    {apps, [platform_epaper_clock]}
]}.
